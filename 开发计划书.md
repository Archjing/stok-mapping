# 跨市场量化研判与选股系统 · 开发计划书

> 项目名称：stok-mapping
> 创建日期：2026-05-28
> 最后修订：2026-05-29（第 7 轮审核 + 离线历史库与指数库落地）
> 状态：规划中
> 法律声明：本工具定位为**量化研究与风险提示工具**，所有输出属于观察池、信号等级、风险暴露和情景推演性质，不构成任何形式的投资建议、荐股或交易指令。使用者应独立判断并承担全部交易风险。
> **边界声明：本系统仅供个人研究和自用决策辅助，不对外提供投资建议或商业服务。**

---

## 一、材料解读摘要

本项目基于两份核心研究材料构建分析框架：

### 1.1 `us-cn-stock-correlation-mapping` 报告

报告给出了一套精确的量化映射框架，核心理念是**"外部冲击决定开盘，内生逻辑决定收盘"**：

**三层映射关系（按可靠性排序）：**

- **🥇 隔夜情绪传导（最稳定、最快）**：纳指 >2% 波动 → A股次日同向开盘概率 >70%。VIX > 25 → 全球 risk-off → A股承压。时滞：T+0（隔夜）。
- **🥈 资金流向（中等稳定，正在减弱）**：美股波动 → 外资减配/北向资金流出 → A股承压。但 2021 年以来外资影响力持续下降。
- **🥉 产业基本面映射（最持久，板块>大盘）**：英伟达→光模块（强）、费城半导体→A股半导体（强）、苹果→果链（强）、特斯拉→新能源（中）。月线级别纳指与上证几乎零相关。

**7 个关键跟踪指标：** 纳斯达克100、费城半导体(SOX)、标普500、VIX、KWEB（中概互联）、美元指数(DXY)、10Y美债收益率。

**A股独立行情的触发条件（A股何时"不跟"）：** 国内政策驱动 > 估值差异 > 外资退潮 > 宏观政策周期分化。

### 1.2 《GPT时代的量化交易》导读

本书提供了量化交易的五大类策略方法论和工程技术基础：

- **五大类策略体系**：基本面量化（巴菲特式）、资产配置量化（达利欧全天候）、阿尔法量化（西蒙斯因子挖掘）、贝塔量化（克罗趋势追踪）、另类量化（索罗斯事件驱动）
- **KISS 原则**：简单策略优于复杂策略。奥卡姆剃刀——参数越少越好。
- **因子建模**：因子 = 能解释股票收益差异的变量。Fama-French 三因子是基础，模型 = 多因子加权组合。
- **凯利公式仓位管理**：f = (bp - q) / b，实践中使用"半凯利"（f/2）降低波动。
- **策略有效性评估**：年化收益率、夏普比率、最大回撤、胜率、盈亏比、Calmar比率。
- **GPT 应用方向**：代码生成、自然语言数据查询、研究报告自动生成、非结构化数据→结构化转换、因子构思辅助。
- **核心哲学**：底层逻辑 > 编程工具。GPT 降低代码门槛，理解策略逻辑才是关键。

### 1.3 两份材料的交叉印证

| 量化理念 | 报告支撑 | 本书支撑 |
|---------|---------|---------|
| 因子思维 | 纳指涨跌幅→A股次日开盘方向（单因子规则） | Fama-French 多因子模型 |
| 概率思维 | "纳指跌>2% → A股低开概率>70%" | 上帝视角假设，没有必然只有大概率 |
| 简单规则 | 隔夜法则只需看纳指/VXV/KWEB 三个数 | KISS 原则，奥卡姆剃刀 |
| 板块思维 | 板块映射远强于大盘映射 | "所有的阿尔法都是贝塔" |
| 信号验证 | 盘中验证隔夜映射是否兑现 + 独立行情检测 | 回测验证，策略有效性评估 |

---

## 二、系统定位

**每日跨市场量化研究与风险提示工具**——利用美股→A股、港股→A股的事实传导映射关系，综合多因子分析，自动生成**每日盘前情景推演、映射观察池、信号等级与风险暴露报告**，通过邮件投递。

核心差异：不是通用量化平台，而是**A 股本土因子为主、跨市场传导为风险/情绪 overlay**的研究工具。输入三市场动态数据，输出 A 股风险情景分析与观察池。

> Phase 0 后调整：回测显示跨市场映射直接作为主选股因子效果弱于简单 A 股动量基线；文献调研也显示美股→A股溢出多在开盘/短时段被吸收，不宜作为主 ranker。因此，项目主线由“跨市场映射选股核心”调整为“本土主因子选股 + 跨市场风险缩放/情绪解释”。

> ⚠️ 定位约束：所有输出使用"信号等级/观察池/风险暴露/情景推演/失效条件"等研究语言，**不使用"强买入/清仓/满仓/荐股"等交易指令语言**。

### 输入/输出全景

```
输入层                      分析引擎层                    输出层
────────────────────────────────────────────────────────────────
美股收盘 → 隔夜映射引擎 → 风险/情绪 overlay ─┐
港股走势 → 港股领先指标 → A股情绪验证        ├→ 综合评分 → 观察池 → 信号等级
A股数据 → 本土因子引擎 → 板块/个股筛选      ─┘
港股实时 → 外资情绪代理 → 方向确认
政策新闻 → NLP 摘要解析 → 权重调整
                                              ↓
                     可成交性模型（T+1/涨跌停/流动性/冲击成本）→ 风险暴露报告
                                              ↓
                  邮件投递 + Web Dashboard（手机/平板/桌面） + PWA 推送
```

---

## 三、三大传导路径的量化落地

### 引擎 #1：隔夜情绪传导（最稳定）

```
输入: 纳指涨跌幅 / SOX涨跌幅 / VIX / KWEB / A50期货
规则:
  if 纳指 > +2%:    A股科技次日高开概率 >65% → 生成"偏多"信号
  if 纳指 < -2%:    A股科技次日低开概率 >70% → 生成"偏空"信号
  if VIX > 25:      全球risk-off → A股承压 → 风险暴露下降信号
  if VIX < 15:      风险偏好正常 → 标准风险暴露
  if KWEB 涨/跌 >2%: 中概情绪传导 → A股情绪同向
输出: 次日开盘方向 + 置信度 + 风险等级（绿/黄/橙/红）
```

### 引擎 #2：本土因子与板块映射引擎（选股核心）

Phase 1 以后，选股核心不再由跨市场映射单独决定，而是优先使用 A 股本土因子：

| 主策略候选 | 数据依赖 | 当前优先级 | 说明 |
|-----------|---------|-----------|------|
| 残差动量 + 短周期反转 | OHLCV + 标的池横截面 | 高 | Phase 0 可立即验证；普通中期动量在 A 股证据弱，短周期动量/反转更值得测试 |
| 价值/质量 + 均线择时 | 财务因子 + OHLCV | 高 | 需补 PE/EP、ROE、盈利能力、流动性、市值数据后进入正式回测 |
| 行业/主题轮动 + 个股确认 | 行业分类 + OHLCV + 基本面 | 中 | 适合 A 股主题交易结构，但必须先补行业分类和行业层回测 |
| 跨市场映射 | 美股/港股/汇率/VIX | Overlay | 用于风险缩放、情绪解释、开盘情景，不作为主排序因子 |

| 美股触发事件 | A股映射标的 | 映射强度 | 触发阈值 |
|-------------|-----------|---------|---------|
| 英伟达(NVDA)涨跌 | 中际旭创/新易盛/天孚通信/中科曙光 | **强** | >3% |
| 费城半导体(SOX)涨跌 | 中芯国际/北方华创/韦尔股份/澜起科技 | **强** | >2% |
| 苹果(AAPL)涨跌/指引 | 立讯精密/歌尔股份/蓝思科技/鹏鼎控股 | **强** | >3% |
| 特斯拉(TSLA)涨跌 | 宁德时代/比亚迪/赣锋锂业/天齐锂业 | **中** | >3% |
| Meta资本开支变化 | 光模块/算力租赁标的 | **中高** | 财报事件 |
| 微软/谷歌涨跌 | 云计算/SaaS/AI应用标的 | **中** | >2% |

### 引擎 #3：港股→A股领先指标

> ⚠️ 关键补充：由于港交所 2024 年 4 月公告调整后，北向实时买入/卖出/总成交额不再提供、持股查询改为季度披露（[HKEX 2024-04-12](https://www.hkex.com.hk/News/Market-Communications/2024/2404122news?sc_lang=en)），港股实时走势成为了解外资对 A 股情绪的最重要**盘中代理指标**。

```
恒生科技指数实时走势 → A股科技板块盘中方向（外资情绪的即时代理）
恒生指数实时走势     → A股大盘盘中情绪
AH溢价指数           → 同一公司A/H股价差 → 估值信号
港股通资金流向(南向) → 内地资金对港股的态度 → 间接反映市场风险偏好
离岸人民币(CNH)实时   → 外资对中国资产的即时定价
```

---

## 四、多因子研究框架

### 4.1 因子体系（初始启发式权重，需回测校准）

> ⚠️ 以下权重为初始启发值。正式使用前必须经过 Phase 0 回测校准，包括：因子标准化（z-score/分位数）、行业/市值中性化、缺失值处理策略、Rank IC 检验、分组收益、因子衰减分析和多重共线性检查（VIF）。综合分数不可直接用于交易决策。

```
单只标的研究评分 = 本土主因子分数 × 跨市场/港股/汇率风险缩放 × 可成交性约束

因子池（Phase 0/1 回测后重新校准）：
├─ 本土价格行为因子 (第一优先级): 残差动量 / 短周期反转 / 低波 / 趋势确认
│   └─ 处理: 横截面标准化；后续补行业/市值中性化
├─ 本土基本面因子 (第二优先级): E/P 或 PE分位 / ROE / 营收/利润增速 / 现金流质量 / 负债率 / 市值 / 流动性
│   └─ 处理: 行业中性化，缺失值用行业中位数填充；当前已接入季度财务因子，但进入历史回测前必须做公告日 point-in-time 校验
├─ 行业/主题轮动因子 (第三优先级): 行业强弱 / 行业内相对强度 / 主题拥挤度
│   └─ 处理: 行业层先排序，再做个股确认
├─ 跨市场风险 overlay: 隔夜映射方向 / VIX / KWEB / CNH / A50
│   └─ 处理: 只做风险缩放、信号解释和开盘情景，不作为主选股 ranker
└─ 新闻摘要因子 (辅助): 政策/财报/研报的结构化摘要
    └─ 处理: LLM 仅做摘要提取，返回结构化 JSON，不生成评分

因子衰减监控: 月度计算 Rank IC，IC<0.02 连续3个月 → 权重减半或移入观察
共线性控制: 两两因子相关性>0.7 → 保留其中一个，或合成单一因子
```

### 4.1.1 文献与 Phase 0 后的策略优先级

| 策略方向 | 采纳状态 | 依据 |
|---------|---------|------|
| 残差动量 + 短周期反转 | Phase 0.1 立即测试 | 近年 A 股研究显示普通中期动量较弱，但短周期动量、反转和残差动量更值得验证 |
| 价值/质量 + 均线择时 | Phase 1 补公告日校验后测试 | 中国特化因子研究支持 E/P、盈利能力、修正规模等本土因子；当前已接入最新横截面财务字段，但历史回测前必须防止未来函数 |
| 行业/主题轮动 + 个股确认 | Phase 1.5 测试 | A 股主题交易明显，但需要可靠行业分类和行业层样本 |
| 跨市场映射主 ranker | 暂不采纳 | Phase 0 回测弱于 A 股简单动量基线，且文献显示溢出多为短时情绪传导 |

### 4.2 信号等级体系（研究输出，非交易指令）

| 信号等级 | 触发条件（参考） | 研究含义 | 风险暴露 | 失效条件 |
|---------|---------------|---------|---------|---------|
| L1-高关注 | 映射正向 + 技术低位 + 外资情绪偏多 + Score>80 | 映射关系强化，纳入核心观察池 | 标准暴露 | 次日映射方向反转 |
| L2-关注 | 映射正向但技术偏高 + Score 60-80 | 映射关系成立，注意追高风险 | 降低暴露 | 技术指标超买+映射减弱 |
| L3-中性 | 映射方向不明 + Score 40-60 | 无明确跨市场信号，观望 | 零暴露 | — |
| L4-谨慎 | 映射负向 + 外资情绪偏空 + Score 20-40 | 跨市场信号不利，关注独立行情可能 | 减暴露 | 国内政策对冲出现 |
| L5-避险 | 映射负向 + 隔夜>3%暴跌 + VIX>30 | 极端风险情景，关注系统性冲击 | 零暴露 | VIX回落+国内政策驱动 |

### 4.3 风险预算体系（替代凯利公式）

> ⚠️ 不采用凯利公式：股票择时信号的胜率和盈亏比分布极不稳定，参数估计误差会导致 Kelly 输出仓位过大。改用多层风险预算约束。

```
风险预算约束（由紧到松）：

第一层 — 硬上限
├─ 单票风险暴露上限: 组合净值的 2%
├─ 单行业/板块上限: 组合净值的 15%
└─ 总风险暴露上限: 组合净值的 80%（永远保留 20% 现金缓冲）

第二层 — 波动率目标
├─ 组合目标年化波动率: 15%
└─ 单票波动率>40% → 排除或暴露为 0

第三层 — 回撤熔断
├─ 组合滚动20日回撤 >8%  → 总暴露减半
├─ 组合滚动20日回撤 >15% → 总暴露降至 20%
└─ 组合滚动20日回撤 >25% → 全部平仓，进入"只观察"模式（至少5个交易日）

第四层 — 连续失败降级
├─ 同一标的连续3次信号被验证为反向 → 暂停该标的信号 10 个交易日
└─ 系统整体胜率滚动30日 <40% → 所有信号降一级
```

### 4.4 可成交性模型（信号→实际执行前的必须校验）

```
每个观察池标的在生成信号前必须通过以下检查：

├─ 停牌检查: 当日是否停牌 → 跳过
├─ 涨跌停检查: 是否一字涨停(不可买)/一字跌停(不可卖) → 跳过当日
├─ T+1 约束 (A股): 当日买入的标的 T+1 才能卖出
├─ 流动性检查: 近20日日均成交额 < 5000万 → 标记"低流动性"，降低暴露
├─ 冲击成本估算: 按当日开盘价计算 1% 成交量冲击 → >0.5% 降低暴露
├─ 开盘追价风险: 集合竞价大幅高开/低开(>3%) → 标记"开盘风险"
├─ 交易成本: 佣金 0.025% + 印花税 0.05%(卖) + 预估滑点 0.1% → 内嵌到回测
└─ 行业集中度: 同一行业暴露 >15% → 等权缩至 15%
```

---

## 五、每日运行时间线

**选择结论：采用“16:00 A股收盘采集 + 06:00 美股收盘采集 + 07:30 盘前研报邮件投递”为主时间线；保留 09:15/09:25 集合竞价修正和即时告警，不把集合竞价信息强行塞进 07:30 研报。**

理由：

- A股 15:00 收盘后，免费/低成本数据源存在延迟和补数，16:00 比原 15:30 更稳，适合作为本土因子、收盘校验和次日候选池的正式入库时间。
- 开发期实际定时增量任务放在 16:30 执行，比 16:00 多留 30 分钟给 AkShare/东方财富类免费源完成收盘后补数；系统仍把 16:00 作为允许写入日线收盘数据的最早时间，避免盘中快照误写。
- 美股收盘对应北京时间夏令时约 04:00、冬令时约 05:00；06:00 采集比原 05:30 更能覆盖冬令时和 yfinance 等数据源延迟，减少“数据还没稳定就生成报告”的风险。
- 07:30 盘前研报比原 06:00 盘前简报更完整，能同时包含上一交易日 A股收盘、本日美股隔夜、汇率/VIX/KWEB/SOX 等跨市场信息；同时比 08:00 更早，给盘前阅读、人工复核和临场准备留出更多时间。
- 原 18:00 报告对次日盘前决策过晚，保留为后台数据归档/异常审计即可，不作为主要邮件投递节点。

```
北京时间  动作                          依赖数据              引擎
───────  ────────────────────────────   ──────────────────  ──────
16:00    抓取A股收盘数据                  akshare/本地缓存      本土因子引擎
         更新日线、可成交性字段、股票池基础特征
16:20    收盘校验                         A股收盘数据           引擎#2
         映射预测 vs 实际走势，更新"独立行情模式"状态
16:30    开发期增量刷新 a_share_history    akshare全市场快照      本地SQLite fallback
         校验最近交易日滞后<=1天、覆盖率>=80%，回填市值/PE/PB/换手率，成功后重建股票池
         东方财富快照断连时尝试新浪原始快照备用源
每周一03:30 财务因子增量刷新              东方财富季度财报接口     market_financial_factors
         刷新最近8个季度ROE/营收利润增速/现金流质量/负债率；避开06:00美股采集、07:30研报和16:30日线增量
06:00    抓取美股收盘数据                 yfinance             引擎#1
         纳指/标普/SOX/VIX/NVDA/KWEB/CNY=X
06:20    运行"隔夜映射引擎"               美股收盘数据          引擎#1
         生成跨市场风险/情绪 overlay
07:00    抓取港股期货/亚太早盘             akshare              —
07:30    生成并邮件投递《每日盘前研判日报》 全量盘前数据          全部引擎
         本土主因子候选池 + 跨市场 overlay + 风险暴露
09:15    A股集合竞价数据                   akshare              —
09:25    集合竞价修正/即时告警             集合竞价结果          可成交性模型
         仅在开盘风险、涨跌停、流动性异常时发增量通知
09:30-   盘中跟踪（可选）：
15:00    · 恒生指数/恒生科技实时走势（外资情绪代理，替代已停发的北向实时数据）
         · 离岸人民币 CNH 汇率波动
         · A50 期货实时走势
         · 涨跌停比例变化
         · 板块轮动/VOL变化
         · 独立行情模式检测
         · 陆股通每日额度使用率（余额<30% 时仍披露）
```

---

## 六、技术栈

### 6.1 完整技术选型

| 层次 | 技术 | 说明 |
|------|------|------|
| 语言 | Python 3.12+ | 主力开发语言 |
| 数据采集（开发/研究） | yfinance + akshare | 免费、适合回测和开发验证 |
| 数据采集（生产适配层） | 抽象 DataSource 接口 | 预留 Tushare Pro / 聚宽 / Wind / 同花顺 iFinD / 交易所授权数据的适配器插槽 |
| 定时任务 | APScheduler | cron 风格调度 |
| 数据存储 | SQLite + Parquet（三层架构） | raw_data → features → signals |
| 回测框架 | pandas + sklearn + walk-forward | 当前 50 只映射池、日频数据、3-5 年历史规模很小，不需要 Qlib；数据规模扩大后再评估 |
| LLM | 配置化模型名（不硬编码），默认 `deepseek/deepseek-v4-pro` | 仅用于：简报摘要润色、政策/研报结构化提取、因子构思辅助；**不直接生成或修改信号** |
| Web 前端 | React + TypeScript + ECharts + Tailwind CSS | Phase 2 响应式 Dashboard，手机/平板/桌面同一套代码 |
| 跨屏方案 | PWA（Progressive Web App） | Phase 2.5 升级：添加到主屏幕、离线缓存、Web Push、底部导航 |
| 桌面端 | Tauri 封装（远期预留） | Phase 4 评估：复用 React 前端，管理本地 Python 服务、系统托盘、本地通知和离线数据查看 |
| 原生 App | Capacitor 封装（远期预留） | 系统级推送/Face ID/小组件需求明确后再评估，不优先开发原生 iOS |
| 部署 | Docker Compose + Nginx | WSL + ECS 通用，Nginx 反代前端静态资源 |
| 安全 | python-dotenv + 操作日志 | .env 不入库，密钥轮换，报告访问权限，操作审计日志 |

### 6.2 从 stok-quant 复用的模块（通过 uv workspace / stok-core 内部包）

> ⚠️ 不直接 import 另一个 repo。采用 uv workspace 或内部包（stok-core）方式引用，避免 Docker/ECS 环境找不到路径。

| 模块 | 路径 | 复用方式 |
|------|------|---------|
| A股数据源 | `backend/markets/cn.py` | 抽取到 stok-core，通过 uv workspace 引用 |
| 港股数据源 | `backend/markets/hk.py` | 同上 |
| 市场注册表 | `backend/markets/registry.py` | 同上 |
| 代码规范化 | `backend/markets/symbols.py` | 同上 |
| 技术分析引擎 | `backend/analyzers/technical.py` | 同上 |
| 回测引擎 | `backend/backtest/engine.py` | Phase 0 回测时引用 |
| 回测指标 | `backend/backtest/metrics.py` | 同上 |
| 市场规则 | `backend/backtest/market_config.py` | CN T+1/涨跌停，HK board lot |
| 数据清洗 | `backend/crawlers/data_cleaner.py` | OHLCV 标准化 |

---

## 七、数据源方案总结

> ⚠️ 以下数据源中，yfinance 和 akshare 定位为**开发和研究环境**。yfinance 自身声明不受 Yahoo 官方认可（"yfinance is not affiliated, endorsed, or vetted by Yahoo, Inc."），偏研究/教育和个人使用。生产级系统需留有官方数据源适配层。

| 数据类型 | 开发/研究源 | 可用性 | 生产适配层（预留） | 调用频率 |
|---------|-----------|--------|-----------------|---------|
| 美股指数 | yfinance | 免费，回测已验证 | Polygon.io / Alpaca / Bloomberg | 每日 1次 |
| 美股个股 | yfinance | 免费 | 同上 | 每日 1次 |
| 中概股/KWEB | yfinance | 免费 | 同上 | 每日 1次 |
| A50期货 | yfinance | 免费 | Wind / 交易所 | 每日 1次 |
| A股日线 | akshare | 免费，三接口fallback | Tushare Pro / Wind / 聚宽 | 每日 1次 |
| A股离线日线 fallback | 手动预下载 SQLite | 已导入 qfq/bfq 最近 10 年，网络失败时用于回测和股票池 fallback | Tushare Pro / Wind / 聚宽 | 按需重建 |
| A股指数日线 fallback | 手动预下载 SQLite | 已导入 995 个指数，含上证/深证/中证指数 | Wind / 中证指数授权数据 | 按需重建 |
| A股股票列表/交易日历/退市清单 | 手动预下载 SQLite | 已导入，用于股票池、时效校验和幸存者偏差控制 | 交易所/Tushare Pro/Wind | 按需重建 |
| A股季度财务因子 | 东方财富数据中心 / AkShare 底层同源接口 | 已接入 ROE、营收/利润增速、现金流质量、负债率；当前用于最新横截面分析 | Tushare Pro / Wind / 聚宽 / 巨潮公告 | 季度/按需 |
| A股实时 | akshare | 免费，有限 | Tushare Pro / 交易所L2 | 盘中 15min |
| 港股日线 | akshare | 免费 | Wind / 港交所 | 每日 1次 |
| 港股实时 | akshare | 免费 | 港交所实时数据 | 盘中 5min |
| 北向资金（盘后日频） | akshare | 日频总量仍可用 | Wind / 港交所CCASS | 收盘后 1次 |
| 北向资金（季度持股） | 港交所CCASS | 季度披露 | — | 季度 1次 |
| 股指期货 | akshare | 免费 | Wind / CFFEX | 每日 1次 |
| 离岸人民币 | yfinance | 免费 | Wind / Bloomberg | 盘中 5min |
| 宏观经济 | akshare/fredapi | 免费 | Wind / CEIC | 每日 1次 |
| 政策新闻 | 爬虫 + LLM 摘要 | Phase 3 | 财新/路透/彭博 | 每日 1次 |

---

## 八、分阶段开发计划

### Phase 0 — 数据验证与策略有效性评估（必须先于任何信号输出，2-3 周）

**目标：输出一份《策略有效性报告》，过线后才能进入 Phase 1 生成真实观察池。**

```
Week 1: 数据可得性验证
├─ 交易日历建立（A股/港股/美股节假日和半天交易日）
├─ 数据源连通性测试（yfinance / akshare 接口调用成功率统计）
├─ 本地离线历史库建立（已落地）
│   ├─ `data/manual_history/a_share_history.sqlite`
│   ├─ A股 qfq/bfq 日线: 最近 10 年
│   ├─ 股票列表、交易日历、退市股票列表
│   ├─ 指数元数据和指数日线（995 个指数）
│   ├─ 季度财务因子表 `market_financial_factors`
│   │   └─ ROE、营收/利润增速、经营现金流/净利润、资产负债率
│   └─ 在线抓取失败时作为回测和股票池 fallback
├─ 数据质量审计
│   ├─ 缺失值比例（每标的每字段）
│   ├─ 异常值检测（OHLCV 逻辑校验：high>=max(open,close) 等）
│   ├─ 复权一致性（前复权 vs 后复权差异）
│   └─ 数据时效性（延迟多久可获取到上一交易日数据）
└─ 输出: 数据源评估报告 + 数据质量评分

当前离线历史库重建命令：

```bash
# 完整重建股票/指数/日历/退市本地库
/home/zj/workspace/stok-quant/.venv/bin/python -m phase0.cli import-history --config config.yaml

# 仅重建指数元数据和指数日线表
/home/zj/workspace/stok-quant/.venv/bin/python -m phase0.cli import-index-history --config config.yaml
```

当前已验证：

- `market_daily_bars`: qfq `10,462,485` 行，bfq `10,462,385` 行。
- `market_index_bars`: `1,876,918` 行 / `995` 个指数。
- `SH.000001` 指数代码前导零丢失问题已修复，当前可通过本地历史接口读取。
- 本地快照时效保护已启用：默认允许最多滞后 1 天，超过则当前股票池 fallback 返回空并告警。

Week 2: 策略回测（Walk-Forward 方法）
├─ 回测周期: 截至最近完整交易日的最近 5 年历史日线数据
├─ 回测方法: 滚动窗口 Walk-Forward（训练 2 年 → 验证 1 年 → 滚动）
├─ 交易成本模拟:
│   ├─ A股: 佣金 0.025% + 卖出印花税 0.05% + 滑点 0.1%
│   ├─ 港股: 佣金 0.03% + 卖出印花税 0.13% + 滑点 0.15%
│   └─ 冲击成本: 按日均成交额的 1% 量估算
├─ 复用 stok-quant backtest/engine.py 的市场规则
│   ├─ A股 T+1 卖出限制
│   ├─ 涨跌停不可成交
│   └─ 停牌处理
└─ 输出: Walk-Forward 回测报告

Week 2-3: 样本外验证 + 验收
├─ 预留最近 6 个月作为样本外测试集（不在任何训练/参数优化中使用）
├─ 验收指标:
│   ├─ 年化收益率 vs 基准（沪深300）
│   ├─ 夏普比率 > 0.5
│   ├─ 最大回撤 < 25%
│   ├─ 胜率 > 45%
│   ├─ 盈亏比 > 1.2
│   ├─ 换手率（年化）< 500%
│   ├─ 滑点敏感性分析（±50%滑点假设）
│   └─ 样本外表现不显著弱于训练期（<30%衰减）
├─ 因子权重校准（基于 Walk-Forward Rank IC）
└─ 输出: 《策略有效性报告》 → 验收通过方能进入 Phase 1

⚠️ 如果不通过: 回到报告中的映射规律，调整阈值/替换因子/缩小标的池，重新走 Phase 0
```

### Phase 1 — 本土主因子验证 + 盘前简报 MVP（3-4 周）

**目标：先确认可替代 `legacy_momentum` 的 A 股本土主策略，再生成每天 07:30 的《每日盘前研判日报》。跨市场信号只做风险/情绪 overlay。**

```
Week 1: 本土主因子候选验证
├─ 实现 residual_momentum_reversal_v1
│   ├─ 残差动量: 个股动量 - 标的池横截面平均动量
│   ├─ 短周期反转过滤: 3日过热标的降权/剔除
│   ├─ 趋势确认: close > MA
│   └─ 跨市场 overlay: 只做风险缩放，不参与主排序
├─ 输出全候选 walk-forward 明细
├─ 与 legacy_momentum、xmarket_* 候选同口径比较
└─ 若未通过: 不进入观察池输出，仅保留为研究候选

Week 1-2: 数据管线搭建
├─ 创建项目骨架（参考 stok-quant 目录结构）
├─ 搭建 data_layer（复用 markets/ + market_data_manager.py 模式）
├─ 接入财务因子更新命令 `update-financials`
│   ├─ 数据源: 东方财富数据中心季度接口
│   ├─ 字段: ROE、营收/利润同比、现金流质量、资产负债率
│   └─ 注意: 当前不改变股票池排序，历史回测前必须做公告日 point-in-time 校验
├─ 编写 config/mapping_pool.yaml（映射标的池，~40 条映射关系）
├─ 实现 engines/overnight_mapper.py（引擎 #1）
│   ├─ 美股指数数据抓取（纳指/标普/SOX/VIX/KWEB）
│   ├─ >2% 阈值规则 + VIX 风险评级
│   └─ 生成隔夜映射信号 JSON
└─ 单元测试：验证纳指涨跌 → 映射信号生成

Week 2-3: 打分模型 + 简报生成
├─ 实现 engines/sector_mapper.py（引擎 #2）
│   ├─ 读取 mapping_pool.yaml
│   ├─ 匹配触发事件的 A 股标的
│   └─ 输出：跨市场情绪解释和风险 overlay
├─ 实现 engines/scoring_model.py V1（多因子打分）
│   ├─ 本土主因子: 残差动量 / 短周期反转 / 低波 / 趋势确认
│   ├─ 跨市场 overlay: VIX / KWEB / CNH / SOX / NVDA
│   └─ 技术面因子 — 复用 technical.py
├─ 实现 report_generator.py（Markdown 模板）
│   ├─ 《每日盘前研判日报》模板
│   └─ 收盘校验/异常审计模板
├─ GPT 润色（可选开关）
└─ 实现 email_sender.py（SMTP，支持 163/QQ/Gmail）

Week 3-4: 定时调度 + 端到端联调
├─ APScheduler 定时任务
│   ├─ 16:00 A股收盘数据采集 + 本土因子更新
│   ├─ 16:30 开发期 a_share_history 增量更新 + 股票池重建
│   ├─ 每周一03:30 财务因子刷新（低优先级、带锁、独立日志）
│   ├─ 06:00 美股收盘数据采集 + 隔夜映射更新
│   ├─ 07:30 日报生成 + 邮件投递
│   └─ 09:15/09:25 集合竞价修正 + 增量告警（可选）
├─ 接入港股数据（引擎 #3 初版）
├─ 端到端测试：完整一天自动运行
├─ Docker Compose 部署脚本
└─ README 文档
```

---

### 移动端策略

**手机定位为"随时看结论"，不是"完整研究工作台"。桌面端保留所有复杂分析和配置功能。**

```
第一阶段（Phase 2）：响应式 Web
├─ 核心报告、信号列表、个股详情在手机浏览器直接可用
├─ React + TypeScript + ECharts + Tailwind CSS，一套代码三屏自适应
├─ 移动端: 卡片式布局、触控友好、底部导航、暗色模式
└─ 桌面端: 复杂图表、回测面板、因子编辑、映射网络图、大表格

第二阶段（Phase 2.5）：PWA 升级
├─ 添加到手机主屏幕（manifest.json）
├─ 离线缓存最近 7 天报告 + 最近信号（service worker，可在设置中关闭）
├─ Web Push 推送（重大隔夜波动、VIX 超阈值、持仓触发风险）
├─ 登录态持久化 + 收藏标的管理 + 提醒阈值设置
└─ 强制 HTTPS

远期（Phase 4+）：原生 iOS App 评估
├─ 触发条件明确才做：系统级推送更稳定 / Face ID / Touch ID / iOS 小组件 / 后台刷新 / Apple Watch
├─ 技术路线：优先 Capacitor 封装现有 Web（维护成本远低于 SwiftUI 重写）
├─ 暂不优先开发——PWA 已覆盖日常使用场景
└─ 原生 App 会使开发和维护成本翻倍，仅在有明确不可替代的原生需求时启动
```

### Phase 2 — 响应式 Web Dashboard（3-4 周）

**目标：一套后端 API 同时服务手机和桌面。手机端定位为"随时看结论"，桌面端定位为"完整研究工作台"。**

```
移动端优先页面（手机浏览器直接可用）：
├─ 今日简报: 大盘方向、风险等级、关键触发事件（卡片式）
├─ 今日信号: L1-L5 信号分布、强信号标的、观察池、谨慎/避险提示
├─ 个股详情: 映射来源、技术面概览、资金面信号、历史命中率
├─ 我的持仓: 与今日信号交叉后的风险提示
└─ 推送/提醒: 重大隔夜波动、VIX 超阈值、持仓触发风险

桌面端专属页面（复杂分析和配置）：
├─ 跨市场映射关系可视化（热力图 / 网络图 / 轨迹图）
├─ 历史回测参数面板（Walk-Forward 窗口配置）
├─ 多因子权重编辑与 Rank IC 监控
├─ 大表格筛选与排序（全量观察池）
└─ 数据同步管理

技术实现：
├─ React + TypeScript + ECharts（延续计划书技术栈）
├─ Tailwind CSS 响应式布局（手机竖屏 → 平板横屏 → 桌面宽屏）
├─ 移动端: 卡片式布局、触控友好交互、底部导航、暗色模式
├─ 同一套 REST API，前端按 viewport 切换页面布局
└─ 暗色模式（手机端省电 + 桌面端看盘习惯）

移动端不做：
├─ 大型热力图（不适合小屏交互）
├─ 复杂回测参数面板
├─ 多因子权重编辑器
├─ 映射网络关系图
└─ 大表格全量筛选
```

### Phase 2.5 — PWA 升级（1-2 周）

**目标：不写原生代码，让 Web 获得接近 App 的体验。**

```
├─ manifest.json → "添加到主屏幕"，自定义图标和启动画面
├─ service worker → 离线缓存（静态资源 + 最近 7 天报告 + 最近信号，可在设置中关闭）
├─ 移动端底部导航栏: 简报 / 信号 / 持仓 / 设置
├─ Web Push API → 重大信号变化即时推送
├─ 登录态持久化（Secure + HttpOnly + SameSite Cookie；localStorage 仅存主题、筛选条件等非敏感 UI 偏好）
├─ 退出登录时清除 IndexedDB / Cache Storage / localStorage 中的本地缓存
├─ 收藏标的管理 + 提醒阈值设置
└─ 强制 HTTPS（PWA 要求）
```

### Phase 3 — 增强与优化（3-4 周）

**目标：轻量 ML 验证 + LLM 摘要提取 + 性能优化**

```
├─ 轻量 ML 验证（不引入 Qlib）
│   ├─ sklearn/lightgbm 训练简单分类器
│   │   └─ 输入：映射信号 + 技术因子 + 市场状态
│   │   └─ 输出：次日映射标的涨跌概率
│   ├─ Walk-forward 交叉验证
│   └─ 对比手动规则 vs ML 模型的 Rank IC 差异
├─ LLM 摘要提取（不直接生成信号）
│   ├─ 政策/财报/研报 → 结构化 JSON 摘要
│   ├─ 模型名配置化（环境变量 LLM_MODEL_NAME）
│   └─ 摘要存入 feature_store，由 scoring_model 读取
├─ 性能优化 + 异常处理
│   ├─ API 限流重试策略（指数退避）
│   ├─ 数据缺失降级方案（用前一日数据 + 标记"陈旧"）
│   ├─ 报告生成失败告警
│   └─ 信号版本管理（每次规则/模型变更增加版本号）
```

### Phase 4 — 持续迭代（长期）

```
├─ 更多映射关系挖掘（基于 Walk-Forward 的因子有效性回溯）
├─ 期货情绪指标增强（A50/IF/IC基差纳入因子体系）
├─ 移动端增强
│   ├─ 原生 iOS App 评估：如有系统级推送 / Face ID / 小组件 / Apple Watch 需求 → Capacitor 封装现有 Web（优先于 SwiftUI 重写）
│   └─ 暂无明确原生需求 → 继续优化 PWA 体验
├─ 桌面版工作台（Tauri）
│   ├─ 复用现有 React 前端，不重写 UI 和业务逻辑
│   ├─ 启动/检查本地 Python FastAPI 服务
│   ├─ 管理本地配置文件、数据目录和日志目录
│   ├─ 系统托盘、桌面通知、开机自启动（可选）
│   └─ 离线查看最近报告、信号历史和本地 Parquet/SQLite 数据
├─ Telegram Bot 推送（备选渠道）
├─ 多设备/多账户隔离（仍限定个人自用）
├─ 更多市场接入（日股/欧股对港股的影响）
└─ 数据规模扩大后重新评估 Qlib 等重型框架
```

---

## 九、安全与合规

### 9.1 密钥与环境变量

```
├─ .env 不入库（.gitignore 强制排除）
├─ .env.example 提供模板
├─ SMTP 密码 / LLM API Key 从环境变量读取，不允许硬编码
├─ 密钥轮换: LLM API Key 每季度更新
└─ 生产环境密钥通过 Docker secrets 或 ECS 安全参数注入
```

### 9.2 操作审计

```
├─ logs/audit.log: 每次信号生成/修改/发送的时间戳、规则版本、模型版本、输入数据版本
├─ 报告访问权限: Web Dashboard / Telegram Bot 必须含认证（JWT / API Token）
└─ 操作日志保留不少于 90 天
```

### 9.3 LLM 使用边界

```
LLM 允许的操作:
├─ 政策/财报/研报的结构化摘要提取（文本→JSON）
├─ 盘前简报的自然语言润色（已有结构化数据→自然语言）
└─ 因子构思辅助（"基于 XX 逻辑，有什么可能的量化因子？"）

LLM 禁止的操作:
├─ 不允许直接生成或修改 L1-L5 信号等级
├─ 不允许修改因子权重
└─ 不允许生成交易建议
```

---

## 十、回测精度与 Qlib 使用策略

### 10.1 回测精度选择

**建议：日线为主，Phase 2 补充 60 分钟线。**

| 精度 | 用途 | 阶段 |
|------|------|------|
| 日线 | 映射信号验证、策略回测、报告生成 | Phase 0 起 |
| 60分钟线 | 盘中买点精调、日内映射验证 | Phase 2 可选 |
| 分钟级 | 暂不采用 | — |

理由：
- 报告中所有映射规律都是日级别（隔夜传导、次日开盘），分钟级数据对核心决策没有增益
- yfinance/akshare 的分钟级数据质量和覆盖度不如日线
- 60 分钟线仅用于优化买入时机（例如低开后的最佳入场时间窗口）

### 10.2 Qlib 引入策略

| Qlib 能力 | 当前匹配度 | 建议 |
|-----------|----------|------|
| Alpha158 因子 | 低 | 当前 50 只映射池、日频、3-5 年样本很小，pandas/sklearn 足够 |
| GBDT/LightGBM 预测 | 中 | Walk-forward + sklearn 更轻量，无需引入 Qlib 框架依赖 |
| 自动化因子挖掘 | 低 | 映射关系已由报告提供，无需自动挖掘 |
| 回测框架 | 低 | 已被 stok-quant backtest/engine.py 覆盖 |

**结论：当前阶段不引入 Qlib。** 数据规模扩大（>500 只标的、分钟级数据、>10 年历史）后再评估。Phase 0/1/2 全部使用 pandas/polars + sklearn/lightgbm + walk-forward 即可。

---

## 十一、期货数据在研判中的角色

**不用于直接交易对冲，而是作为领先情绪指标纳入引擎 #1：**

| 期货指标 | 信号含义 | 数据源 |
|---------|---------|--------|
| 新加坡A50期货（盘前） | 外资对次日A股的方向押注 | yfinance |
| 沪深300期货升贴水 | 聪明钱的多空倾向 | akshare |
| 中证500/1000期货基差 | 中小盘风险偏好 | akshare |
| 恒生指数期货（盘前） | 港股开盘方向先行指标 | yfinance |

这些数据 yfinance + akshare 都能获取，作为引擎 #1 的增强输入，无需独立模块。

---

## 十二、项目目录结构

```
stok-mapping/
├── 开发计划书.md                  # 本文档
├── README.md                      # 项目说明
├── pyproject.toml                 # uv/Python 项目配置
├── config.yaml                    # 项目配置（不含密钥）
├── .env.example                   # 环境变量模板（密钥不在此文件）
├── .gitignore                     # 排除 .env / data/raw_data/ / __pycache__
├── docker-compose.yml             # 容器编排
├── Dockerfile                     # 后端镜像
│
├── config/
│   └── mapping_pool.yaml          # 映射标的池配置（核心）
│
├── engines/                       # 核心分析引擎（新建）
│   ├── __init__.py
│   ├── overnight_mapper.py        # 引擎 #1: 隔夜情绪传导
│   ├── sector_mapper.py           # 引擎 #2: 板块映射
│   ├── hk_leading.py              # 引擎 #3: 港股领先指标
│   ├── scoring_model.py           # 多因子打分模型（含中性化/标准化）
│   ├── signal_generator.py        # 信号等级生成（L1-L5）
│   └── tradability.py             # 可成交性模型
│
├── data_layer/                    # 数据层（三层架构）
│   ├── __init__.py
│   ├── source_interface.py        # DataSource 抽象接口
│   ├── adapters/
│   │   ├── yfinance_adapter.py    # 开发/研究环境适配器
│   │   ├── akshare_adapter.py     # 开发/研究环境适配器
│   │   └── tushare_adapter.py     # 生产环境适配器（预留）
│   ├── raw_store.py               # raw_data 层：原始快照存储
│   ├── feature_store.py           # features 层：标准化因子
│   ├── signal_store.py            # signals 层：信号+元数据
│   ├── trading_calendar.py        # 三市场交易日历
│   └── quality_checker.py         # 数据质量校验
│
├── reporters/                     # 报告生成
│   ├── __init__.py
│   ├── briefing.py                # 盘前情景推演报告
│   ├── daily_report.py            # 每日研判报告
│   └── email_sender.py            # 邮件投递（SMTP 配置从 .env 读取）
│
├── scheduler/                     # 定时任务
│   ├── __init__.py
│   └── jobs.py                    # APScheduler 任务定义
│
├── web/                           # Web Dashboard（Phase 2）
│   ├── backend/                   # FastAPI 后端
│   │   ├── main.py
│   │   ├── auth.py                # 认证中间件
│   │   └── routers/
│   └── frontend/                  # React 前端
│       └── src/
│
├── tests/                         # 测试
│   ├── test_overnight_mapper.py
│   ├── test_sector_mapper.py
│   ├── test_scoring_model.py
│   ├── test_tradability.py
│   └── test_data_quality.py
│
├── data/                          # 本地数据（.gitignore）
│   ├── raw_data/                  # 原始快照（Parquet）
│   ├── features/                  # 特征表（Parquet）
│   ├── manual_history/            # 手动预下载历史数据 SQLite（不入 Git）
│   │   ├── README.md              # 数据库表、来源、重建命令说明
│   │   └── a_share_history.sqlite # A股股票/指数/日历/退市本地 fallback
│   └── signals.db                 # 信号历史（SQLite，含 model_version / rule_version / input_data_version）
│
└── logs/                          # 操作日志
    ├── audit.log                   # 信号生成/修改/发送审计日志
    └── system.log                  # 系统运行日志
```

---

## 十三、部署方案

### WSL 本机部署

```bash
# 开发模式
uv sync
uv run python -m scheduler.jobs    # 只跑定时任务（无 Web）
# 或
uv run uvicorn web.backend.main:app --reload  # Web Dashboard

# 生产模式
docker-compose up -d
```

### ECS 远程部署

```bash
# 与 WSL 完全相同的 Docker Compose 流程
git clone <repo>
cd stok-mapping
docker-compose up -d

# 仅需调整：
# - config.yaml 中的 email SMTP 配置
# - .env 中的 API Key 和密钥
# - 域名解析到 ECS IP → Nginx 反代前端
# - SSL 证书（Let's Encrypt）→ PWA 要求 HTTPS
```

---

## 十四、关键决策记录

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 产品定位 | 量化研究与风险提示工具 | 避免误导为"荐股系统"，所有输出为研究性质 |
| 输出语言 | 信号等级/观察池/风险暴露/情景推演/失效条件 | 不使用"强买入/清仓/满仓"等交易指令语言 |
| 信号先行回测 | 必须 Phase 0 先回测再发信号 | Walk-Forward 3-5 年 + 样本外验证 + 通过验收 |
| 选股范围 | 映射板块标的池（~50 只） | 板块映射远强于大盘映射，聚焦才有精度 |
| 跨屏方案 | PWA + 响应式 | Tailwind CSS 响应式 + PWA 离线缓存 + Web Push |
| 桌面版 | Phase 4 用 Tauri 封装现有 React 前端 | 不维护第二套 UI；桌面只增强本地服务管理、托盘、通知和离线数据查看 |
| 港股用途 | 仅作为 A 股领先指标 | 不独立分析港股选股 |
| 回测精度 | 日线为主 | 映射规律是日级别，分钟级无增益 |
| 仓位管理 | 风险预算体系（非凯利公式） | 凯利公式对择时信号噪声过于敏感 |
| 因子权重 | 先启发后校准 | Phase 0 通过 Walk-Forward Rank IC 重新校准 |
| Qlib | 不引入 | 当前数据规模太小，pandas/sklearn 足够 |
| LLM | 仅摘要/润色，不生成信号，模型名配置化 | 防硬编码和越权 |
| 期货数据 | 作为情绪指标，不做交易 | A50/基差提供盘前方向信号 |
| 北向资金 | 盘后日频总量 + 季度持股（辅助），盘中用港股/CNH替代 | HKEX 2024年4月起停发实时买卖/成交额 |
| stok-quant 复用 | uv workspace 或 stok-core 内部包 | 不直接 import 另一个 repo |
| 部署方式 | Docker Compose | WSL + ECS 完全统一 |
| 数据存储 | raw_data(Parquet) → features(Parquet) → signals(SQLite) | 三层架构，信号带版本追溯 |
| 数据源 | yfinance/akshare 为开发研究源，生产预留适配层 | yfinance 不受 Yahoo 官方认可，仅适合研究 |
| 安全 | .env 不入库、操作审计日志、Web 认证、密钥轮换 | 防止密钥泄露和越权操作 |

---

## 十五、下一步行动

- [ ] 确认映射标的池范围（是否需要增减标的）
- [ ] 确认邮件 SMTP 配置（163/QQ/Gmail/企业邮箱）
- [ ] 确认 LLM API Key / Base URL / 模型名配置可用性（可选，用于简报润色）
- [ ] Phase 0 开工：数据源验证 → 回测框架 → 策略有效性报告
