# 网站导航重构 + 多主题 UI 迁移（双层导航 · 六域路由 · nous/belafonte 主题）实施计划

> **For Hermes:** Use subagent-driven-development skill to implement this plan task-by-task.

**Goal:** ① 双层导航——侧栏（第一层，6 域，可收起为 0 宽度仅留把手）+ 顶栏（第二层，域内子页 tab）；`/` 重定向 `/market/cn`；6 域路由就位，未实施域占位。② UI 多主题迁移——语义化 token（`--ui-*`）+ 主题注册表（**nous** 默认 + **Belafonte** 保留），主题选择器切换，旧变量别名兼容。

**Architecture:** 导航数据抽成单一数据源 `nav.ts`；Layout 用 `useLocation()` 推导当前域；侧栏收起状态存 `localStorage`。主题模型 = `{ themeId: 'nous'|'belafonte', mode: 'dark'|'light' }`，HTML 上 `data-theme` + `data-mode` 双属性；`pal(theme)` 签名不变（参数升级为对象），图表调用点零改动；旧 `index-chart-theme` localStorage key 兼容迁移。现有 `/market/*` 三页零改动只换外壳。后端不动。

**Tech Stack:** React 18 + TypeScript + react-router-dom v6 + Vite（无测试框架，验证 = `tsc -b` + `vite build` + 浏览器手动验证 + curl 深链验证）。

**依据设计文档:** `docs/WEBSITE_NAVIGATION_STRUCTURE.md` §2/§3/§4/§8（导航）与 §10（UI token 规范 + 主题注册表，唯一事实源）。

---

## 前置事实（已核实，实现者无需重复勘察）

- 现有文件：`web/ui/src/App.tsx`（26 行）、`components/Layout.tsx`（98 行，单层顶栏）、`components/ThemeContext.tsx`（`Theme = 'dark'|'light'`）、`pages/HomePage.tsx`（将删除）、`chart/theme.ts`（THEMES dark/light + `pal(theme)`）、`index.css`（变量区文件头 1–22 行）
- `pal(theme)` 调用点：`KLineChart.tsx`（约 8 处）、`ComparisonDashboard.tsx`（2 处）；theme 经 prop 传入（SingleIndexPage/CnDashPage 用 `useTheme()` 取值后传 prop）——**升级 pal 参数类型后调用点语法不变**
- `Navbar/Brand/ThemeToggle` 仅 Layout 内部使用；`Layout` 仅 App.tsx 引用（已 grep 确认）
- **两套主题完整色值表见 `docs/WEBSITE_NAVIGATION_STRUCTURE.md` §10.1**（nous 来源 Hermes presets.ts；belafonte 来源 `quant/reporting/static/style.css`）——实现以文档为准
- 工作区有未提交的无关改动——**每次 commit 只 add 本次文件**，严禁 `git add -A`
- 无 vitest/jest——验证走构建 + 手动 + curl
- 涨红跌绿为项目口径（色值随主题：nous dark `#ef232a`/`#14b143`，belafonte night `#d94a48`/`#14b143`），口径不可改
- 主题默认：nous + dark（与现有默认一致，用户无感）

---

## Task 1: 创建导航数据源 `nav.ts`

**Objective:** 单一数据源：6 域 + 每域子页 + 默认页 + `domainForPath()`。

**Files:**
- Create: `web/ui/src/lib/nav.ts`

**Step 1: 写文件**

```ts
/** 网站导航单一数据源：6 域（首层）→ 每域子页（第二层顶栏 tab）。
 * 设计依据：docs/WEBSITE_NAVIGATION_STRUCTURE.md §3/§4。
 */
export type DomainId = 'market' | 'research' | 'intel' | 'accounts' | 'config' | 'ops';

export interface NavPage {
  to: string;
  label: string;
}

export interface Domain {
  id: DomainId;
  label: string;
  icon: string;
  defaultPath: string;
  pages: NavPage[];
}

export const DOMAINS: Domain[] = [
  {
    id: 'market', label: '行情', icon: '📈', defaultPath: '/market/cn',
    pages: [
      { to: '/market/cn', label: 'A股单标的' },
      { to: '/market/dash', label: 'A股对照' },
      { to: '/market/us', label: '美股单标的' },
    ],
  },
  {
    id: 'research', label: '研究', icon: '🔬', defaultPath: '/research/strategies',
    pages: [
      { to: '/research/strategies', label: '策略' },
      { to: '/research/runs/new', label: '回测' },
      { to: '/research/admission', label: '准入' },
      { to: '/research/runs', label: '对比' },
    ],
  },
  {
    id: 'intel', label: '情报', icon: '📰', defaultPath: '/intel/candidates',
    pages: [
      { to: '/intel/candidates', label: '候选' },
      { to: '/intel/corpus', label: '语料' },
      { to: '/intel/signals', label: '信号' },
    ],
  },
  {
    id: 'accounts', label: '账户', icon: '💰', defaultPath: '/accounts',
    pages: [
      { to: '/accounts', label: '总览' },
      { to: '/accounts/premarket', label: '盘前观察池' },
    ],
  },
  {
    id: 'config', label: '配置', icon: '⚙️', defaultPath: '/config/strategies',
    pages: [
      { to: '/config/strategies', label: '策略参数' },
      { to: '/config/accounts', label: '账户设置' },
      { to: '/config/markets', label: '行情池' },
      { to: '/config/system', label: '系统' },
    ],
  },
  {
    id: 'ops', label: '观测', icon: '🛰️', defaultPath: '/ops/jobs',
    pages: [
      { to: '/ops/jobs', label: '任务' },
      { to: '/ops/scheduler', label: '调度' },
      { to: '/ops/data-health', label: '数据健康' },
    ],
  },
];

/** 按 pathname 推导当前域（/market/us → market 域）。 */
export function domainForPath(pathname: string): Domain | undefined {
  const seg = pathname.split('/')[1];
  return DOMAINS.find((d) => d.id === seg);
}
```

**Step 2: 类型检查**

Run: `cd /Users/aj/workspace/stok-mapping/web/ui && npx tsc --noEmit`
Expected: 无错误。

**Step 3: Commit**

```bash
git add web/ui/src/lib/nav.ts
git commit -m "feat(web/ui): 新增导航单一数据源 nav.ts（6 域 + 子页 + domainForPath）"
```

---

## Task 2: 创建占位页组件 `PlaceholderPage`

**Objective:** 未实施域页面的统一占位组件。

**Files:**
- Create: `web/ui/src/components/PlaceholderPage.tsx`

**Step 1: 写文件**

```tsx
/** 未实施页面的统一占位：标题 + 实施说明。 */
export function PlaceholderPage({ title, desc }: { title: string; desc: string }) {
  return (
    <div className="page-view">
      <div className="view-head">
        <h2>{title}</h2>
      </div>
      <p className="placeholder-note">{desc}</p>
    </div>
  );
}
```

**Step 2: 类型检查**

Run: `cd /Users/aj/workspace/stok-mapping/web/ui && npx tsc --noEmit`
Expected: 无错误。

**Step 3: Commit**

```bash
git add web/ui/src/components/PlaceholderPage.tsx
git commit -m "feat(web/ui): 新增 PlaceholderPage 占位组件"
```

---

## Task 3: 主题模型升级 —— theme.ts 多主题 + ThemeContext 扩展

**Objective:** `Theme` 字符串 → `AppTheme { themeId, mode }`；`THEMES` 变二维注册表；**`pal(theme)` 签名保持**（参数类型升级），图表调用点语法零改动。

**Files:**
- Modify: `web/ui/src/chart/theme.ts`
- Modify: `web/ui/src/components/ThemeContext.tsx`

**Step 1: 重写 `chart/theme.ts`**

```ts
import type { MaSpan } from '../lib/dashboard';

export type Mode = 'dark' | 'light';
export type ThemeId = 'nous' | 'belafonte';

/** 主题选择：主题注册表 id + 明暗模式。 */
export interface AppTheme {
  themeId: ThemeId;
  mode: Mode;
}

export interface ThemePalette {
  bg: string;
  panel: string;
  border: string;
  text: string;
  dim: string;
  axisLine: string;
  splitLine: string;
  tooltipBg: string;
  tooltipBorder: string;
  up: string;
  down: string;
  ma: Record<MaSpan, string>;
}

/** 主题注册表（每主题每模式一套；与 index.css --ui-* 同步维护，CSS 为事实源）。
 * 完整色值表：docs/WEBSITE_NAVIGATION_STRUCTURE.md §10.1 */
export const THEMES: Record<ThemeId, Record<Mode, ThemePalette>> = {
  nous: {
    dark: {
      bg: '#12378f', panel: '#12378f', border: '#3158ad',
      text: '#ffe6cb', dim: '#b5c7f3',
      axisLine: '#3a63bd', splitLine: '#234a9c',
      tooltipBg: '#183f9a', tooltipBorder: '#3158ad',
      up: '#ef232a', down: '#14b143',
      ma: { 5: '#f6d365', 10: '#ff8fab', 20: '#b388ff', 30: '#6ee7b7', 60: '#4fc3f7' },
    },
    light: {
      bg: '#ffffff', panel: '#ffffff', border: 'rgba(0,83,253,0.22)',
      text: '#17171a', dim: '#666678',
      axisLine: '#0053fd', splitLine: 'rgba(0,83,253,0.24)',
      tooltipBg: '#f2f5ff', tooltipBorder: 'rgba(0,83,253,0.22)',
      up: '#dc2626', down: '#16a34a',
      ma: { 5: '#eab308', 10: '#ec4899', 20: '#8b5cf6', 30: '#10b981', 60: '#0ea5e9' },
    },
  },
  belafonte: {
    dark: {
      bg: '#281822', panel: '#281822', border: '#3d2d36',
      text: '#b88f55', dim: '#96754e',
      axisLine: '#3d2d36', splitLine: '#2a1e26',
      tooltipBg: '#2a1e26', tooltipBorder: '#3d2d36',
      up: '#d94a48', down: '#14b143',
      ma: { 5: '#f6d365', 10: '#ff8fab', 20: '#b388ff', 30: '#6ee7b7', 60: '#4fc3f7' },
    },
    light: {
      bg: '#ded8c8', panel: '#ded8c8', border: '#b8b0a4',
      text: '#45373c', dim: '#5e5252',
      axisLine: '#8a827b', splitLine: '#e8e4dc',
      tooltipBg: '#f3ead6', tooltipBorder: '#b8b0a4',
      up: '#be100e', down: '#16a34a',
      ma: { 5: '#d08b30', 10: '#8b5cf6', 20: '#426a79', 30: '#16a34a', 60: '#0ea5e9' },
    },
  },
};

export function pal(t: AppTheme): ThemePalette {
  return THEMES[t.themeId][t.mode];
}
```

注意：调用点 `pal(theme)` 语法不变，但 `theme` 变量类型从字符串变 `AppTheme` 对象——**先改 theme.ts 再改 ThemeContext，中间状态 tsc 会报错，属预期**；两个文件同任务内完成。

**Step 2: 重写 `ThemeContext.tsx`**

```tsx
import { createContext, useContext } from 'react';
import type { AppTheme } from '../chart/theme';

const THEME_KEY = 'website-theme';
const LEGACY_KEY = 'index-chart-theme';

export const DEFAULT_THEME: AppTheme = { themeId: 'nous', mode: 'dark' };

/** 读取持久化主题；兼容旧 key index-chart-theme（dark/light → nous + 对应 mode）。 */
export function loadTheme(): AppTheme {
  try {
    const raw = localStorage.getItem(THEME_KEY);
    if (raw) {
      const t = JSON.parse(raw) as AppTheme;
      if ((t.themeId === 'nous' || t.themeId === 'belafonte') && (t.mode === 'dark' || t.mode === 'light')) {
        return t;
      }
    }
    const legacy = localStorage.getItem(LEGACY_KEY);
    if (legacy === 'dark' || legacy === 'light') {
      return { themeId: 'nous', mode: legacy };
    }
  } catch {
    /* localStorage 不可用时用默认 */
  }
  return DEFAULT_THEME;
}

export function saveTheme(t: AppTheme): void {
  try {
    localStorage.setItem(THEME_KEY, JSON.stringify(t));
  } catch {
    /* 忽略 */
  }
}

/** 应用到 <html>：data-theme + data-mode 双属性（CSS 选择器 [data-theme][data-mode]）。 */
export function applyRootTheme(t: AppTheme): void {
  document.documentElement.dataset.theme = t.themeId;
  document.documentElement.dataset.mode = t.mode;
}

const ThemeContext = createContext<AppTheme>(DEFAULT_THEME);

export function useTheme(): AppTheme {
  return useContext(ThemeContext);
}

export { ThemeContext };
```

**Step 3: 适配图表组件 prop 类型（编译通过即可，逻辑不动）**

Run: `cd /Users/aj/workspace/stok-mapping/web/ui && npx tsc --noEmit`
若报错，将 `KLineChart.tsx` / `ComparisonDashboard.tsx` 中 `theme: Theme` 类型的 prop 改为 `theme: AppTheme`（import 相应类型）；`SingleIndexPage.tsx` / `CnDashPage.tsx` 的 `useTheme()` 用法不变。
Expected: tsc 通过。

**Step 4: Commit**

```bash
git add web/ui/src/chart/theme.ts web/ui/src/components/ThemeContext.tsx web/ui/src/components/KLineChart.tsx web/ui/src/components/ComparisonDashboard.tsx
git commit -m "feat(web/ui): 主题模型升级为注册表（nous/belafonte × dark/light），pal 签名兼容"
```

---

## Task 4: 创建 ThemeSwitcher 组件

**Objective:** 顶栏主题按钮 + 弹出菜单（4 组合），浮动层规范（shadow-nous + stroke-nous）。

**Files:**
- Create: `web/ui/src/components/ThemeSwitcher.tsx`

**Step 1: 写文件**

```tsx
import { useEffect, useRef, useState } from 'react';
import type { AppTheme, Mode, ThemeId } from '../chart/theme';

const OPTIONS: Array<{ themeId: ThemeId; mode: Mode; label: string }> = [
  { themeId: 'nous', mode: 'dark', label: 'Nous Dark' },
  { themeId: 'nous', mode: 'light', label: 'Nous Light' },
  { themeId: 'belafonte', mode: 'dark', label: 'Belafonte Night' },
  { themeId: 'belafonte', mode: 'light', label: 'Belafonte Day' },
];

/** 顶栏主题选择器：按钮显示当前主题名，点击弹出 4 项菜单。 */
export function ThemeSwitcher({
  theme,
  onChange,
}: {
  theme: AppTheme;
  onChange: (t: AppTheme) => void;
}) {
  const [open, setOpen] = useState(false);
  const rootRef = useRef<HTMLDivElement>(null);
  const current = OPTIONS.find((o) => o.themeId === theme.themeId && o.mode === theme.mode) ?? OPTIONS[0];

  // 点击外部关闭菜单
  useEffect(() => {
    if (!open) return;
    const onDocClick = (e: MouseEvent) => {
      if (rootRef.current && !rootRef.current.contains(e.target as Node)) setOpen(false);
    };
    document.addEventListener('click', onDocClick);
    return () => document.removeEventListener('click', onDocClick);
  }, [open]);

  return (
    <div className="theme-switcher" ref={rootRef}>
      <button className="icon-btn theme-switcher-btn" onClick={() => setOpen((v) => !v)} title="切换主题">
        🎨 {current.label}
      </button>
      {open && (
        <div className="theme-menu">
          {OPTIONS.map((o) => (
            <button
              key={`${o.themeId}-${o.mode}`}
              className={`theme-menu-item${o.themeId === theme.themeId && o.mode === theme.mode ? ' active' : ''}`}
              onClick={() => {
                onChange({ themeId: o.themeId, mode: o.mode });
                setOpen(false);
              }}
            >
              {o.label}
            </button>
          ))}
        </div>
      )}
    </div>
  );
}
```

**Step 2: Commit**

```bash
git add web/ui/src/components/ThemeSwitcher.tsx
git commit -m "feat(web/ui): 新增 ThemeSwitcher 主题选择器（nous/belafonte × 明暗）"
```

---

## Task 5: index.css 多主题 token 迁移

**Objective:** 变量区重构：`:root` 语义常量 + 旧变量别名（惰性引用）+ 4 个 `[data-theme][data-mode]` 主题值块。

**Files:**
- Modify: `web/ui/src/index.css`（文件头 1–22 行）

**Step 1: 替换变量区**

将现有 `[data-theme='dark'] { --bg: #0b0f17; ... }` + `[data-theme='light'] { ... }` 整体替换为（完整色值以 `docs/WEBSITE_NAVIGATION_STRUCTURE.md` §10.1 为准）：

```css
/* ═══════════════════════════════════════════
   UI token 体系 — 语义层 --ui-* + 主题注册表值层
   主题：nous（Hermes 默认）/ belafonte（静态站点原主题）
   事实源：docs/WEBSITE_NAVIGATION_STRUCTURE.md §10
   ═══════════════════════════════════════════ */
:root {
  --ui-radius-scalar: 1;
  --ui-stroke-nous: color-mix(in srgb, currentColor 3%, transparent);

  /* 旧变量别名（惰性引用 --ui-*，随主题块解析；迁移完成后逐步移除） */
  --bg: var(--ui-bg-app);
  --panel: var(--ui-bg-panel);
  --panel2: var(--ui-bg-subtle);
  --border: var(--ui-stroke-tertiary);
  --text: var(--ui-text-primary);
  --dim: var(--ui-text-secondary);
  --accent: var(--ui-accent);
  --accent-text: var(--ui-accent-fg);
  --chip-bg: var(--ui-bg-subtle);
  --warn: var(--ui-warm);
  --shadow: var(--ui-shadow-nous);
}

/* ── nous dark：品牌蓝 + 奶油 ── */
[data-theme='nous'][data-mode='dark'] {
  --ui-bg-app: #0d2f86; --ui-bg-panel: #12378f; --ui-bg-subtle: #183f9a; --ui-bg-quaternary: #1b45a4;
  --ui-text-primary: #ffe6cb; --ui-text-secondary: #b5c7f3; --ui-text-tertiary: #8f9fd8;
  --ui-stroke-primary: #3a63bd; --ui-stroke-secondary: #234a9c; --ui-stroke-tertiary: #3158ad;
  --ui-accent: #ffe6cb; --ui-accent-fg: #0d2f86; --ui-accent-secondary: #1540b1;
  --ui-danger: #c0473a; --ui-warm: #e8a33d;
  --ui-up: #ef232a; --ui-down: #14b143;
  --ui-ma-5: #f6d365; --ui-ma-10: #ff8fab; --ui-ma-20: #b388ff; --ui-ma-30: #6ee7b7; --ui-ma-60: #4fc3f7;
  --ui-shadow-nous: 0 6px 28px rgba(0, 0, 0, 0.45);
}

/* ── nous light：冷白 + Nous 蓝 ── */
[data-theme='nous'][data-mode='light'] {
  --ui-bg-app: #f8faff; --ui-bg-panel: #ffffff; --ui-bg-subtle: #f2f5ff; --ui-bg-quaternary: #eff4ff;
  --ui-text-primary: #17171a; --ui-text-secondary: #666678; --ui-text-tertiary: #9aa0b4;
  --ui-stroke-primary: rgba(0, 83, 253, 0.30); --ui-stroke-secondary: rgba(0, 83, 253, 0.24); --ui-stroke-tertiary: rgba(0, 83, 253, 0.22);
  --ui-accent: #0053fd; --ui-accent-fg: #fcfcfc; --ui-accent-secondary: #e6eeff;
  --ui-danger: #c72e4d; --ui-warm: #e8a33d;
  --ui-up: #dc2626; --ui-down: #16a34a;
  --ui-ma-5: #eab308; --ui-ma-10: #ec4899; --ui-ma-20: #8b5cf6; --ui-ma-30: #10b981; --ui-ma-60: #0ea5e9;
  --ui-shadow-nous: 0 6px 28px rgba(15, 23, 42, 0.12);
}

/* ── belafonte night：深酒红 + 金色（静态站点原主题） ── */
[data-theme='belafonte'][data-mode='dark'] {
  --ui-bg-app: #20111b; --ui-bg-panel: #281822; --ui-bg-subtle: #2a1e26; --ui-bg-quaternary: #3d2d36;
  --ui-text-primary: #b88f55; --ui-text-secondary: #96754e; --ui-text-tertiary: #6f5a43;
  --ui-stroke-primary: #4a3a44; --ui-stroke-secondary: #3d2d36; --ui-stroke-tertiary: #3d2d36;
  --ui-accent: #5a8a9a; --ui-accent-fg: #7fb4c4; --ui-accent-secondary: #233840;
  --ui-danger: #d94a48; --ui-warm: #eaa549;
  --ui-up: #d94a48; --ui-down: #14b143;
  --ui-ma-5: #f6d365; --ui-ma-10: #ff8fab; --ui-ma-20: #b388ff; --ui-ma-30: #6ee7b7; --ui-ma-60: #4fc3f7;
  --ui-shadow-nous: 0 1px 3px rgba(0, 0, 0, 0.25);
}

/* ── belafonte day：暖羊皮纸 ── */
[data-theme='belafonte'][data-mode='light'] {
  --ui-bg-app: #fffaed; --ui-bg-panel: #ded8c8; --ui-bg-subtle: #e8e4dc; --ui-bg-quaternary: #ccc5b8;
  --ui-text-primary: #45373c; --ui-text-secondary: #5e5252; --ui-text-tertiary: #8a827b;
  --ui-stroke-primary: #8a827b; --ui-stroke-secondary: #b8b0a4; --ui-stroke-tertiary: #b8b0a4;
  --ui-accent: #426a79; --ui-accent-fg: #2d4d59; --ui-accent-secondary: #cbd5d9;
  --ui-danger: #be100e; --ui-warm: #d08b30;
  --ui-up: #be100e; --ui-down: #16a34a;
  --ui-ma-5: #d08b30; --ui-ma-10: #ec4899; --ui-ma-20: #8b5cf6; --ui-ma-30: #10b981; --ui-ma-60: #0ea5e9;
  --ui-shadow-nous: 0 1px 2px rgba(69, 55, 60, 0.06);
}
```

注意：别名只定义一次（`:root`），CSS 变量惰性解析保证各主题块生效；`--ui-stroke-primary` belafonte night 的 `#4a3a44` 为派生值（介于 border 与 text 间），目检可微调。

**Step 2: Commit**

```bash
git add web/ui/src/index.css
git commit -m "feat(web/ui): 多主题 token 迁移（nous/belafonte × 明暗四组合 + 旧变量别名）"
```

---

## Task 6: 重构 `Layout.tsx` 为双层导航（集成 ThemeSwitcher）

**Objective:** 侧栏（6 域 + 收起）+ 顶栏（域内 tab + ThemeSwitcher）；主题状态用新模型。

**Files:**
- Modify: `web/ui/src/components/Layout.tsx`（整体替换）

**Step 1: 整体重写**

```tsx
import { Link, NavLink, Outlet, useLocation, useNavigation } from 'react-router-dom';
import { useEffect, useState } from 'react';
import type { AppTheme } from '../chart/theme';
import { ThemeContext, applyRootTheme, loadTheme, saveTheme } from './ThemeContext';
import { ThemeSwitcher } from './ThemeSwitcher';
import { DOMAINS, domainForPath, type Domain } from '../lib/nav';

const SIDEBAR_KEY = 'website-sidebar-collapsed';

function initialCollapsed(): boolean {
  try {
    return localStorage.getItem(SIDEBAR_KEY) === '1';
  } catch {
    return false;
  }
}

/** 侧栏（第一层）：领域切换；收起后宽度 0，由 .sidebar-handle 展开。 */
function Sidebar({ collapsed, onToggle }: { collapsed: boolean; onToggle: () => void }) {
  const location = useLocation();
  const activeId = domainForPath(location.pathname)?.id;
  return (
    <aside className={`sidebar${collapsed ? ' collapsed' : ''}`}>
      <div className="sidebar-head">
        <Link to="/market/cn" className="sidebar-brand">
          stok-mapping
        </Link>
        <button className="icon-btn sidebar-toggle" onClick={onToggle} title={collapsed ? '展开侧栏' : '收起侧栏'}>
          {collapsed ? '▶' : '◀'}
        </button>
      </div>
      <nav className="sidebar-nav">
        {DOMAINS.map((d) => (
          <NavLink
            key={d.id}
            to={d.defaultPath}
            className={`sidebar-item${activeId === d.id ? ' active' : ''}`}
            title={d.label}
          >
            <span className="sidebar-icon">{d.icon}</span>
            <span className="sidebar-label">{d.label}</span>
          </NavLink>
        ))}
      </nav>
    </aside>
  );
}

/** 顶栏（第二层）：当前域的子页 tab。 */
function TopNav({ domain }: { domain: Domain }) {
  const nav = useNavigation();
  return (
    <nav className={`topnav${nav.state === 'loading' ? ' loading' : ''}`}>
      {domain.pages.map((p) => (
        <NavLink
          key={p.to}
          to={p.to}
          end={p.to === domain.defaultPath}
          className={({ isActive, isPending }) =>
            isPending ? 'topnav-link pending' : isActive ? 'topnav-link active' : 'topnav-link'
          }
        >
          {p.label}
        </NavLink>
      ))}
    </nav>
  );
}

/** 全局布局：侧栏（首层）+ 顶栏（第二层）+ 内容区（<Outlet />）。 */
export function Layout() {
  const [theme, setTheme] = useState<AppTheme>(loadTheme);
  const [collapsed, setCollapsed] = useState<boolean>(initialCollapsed);
  const location = useLocation();
  const domain = domainForPath(location.pathname) ?? DOMAINS[0];

  useEffect(() => {
    applyRootTheme(theme);
  }, [theme]);

  useEffect(() => {
    saveTheme(theme);
  }, [theme]);

  useEffect(() => {
    try {
      localStorage.setItem(SIDEBAR_KEY, collapsed ? '1' : '0');
    } catch {
      /* 忽略 */
    }
  }, [collapsed]);

  return (
    <ThemeContext.Provider value={theme}>
      <div className={`page${collapsed ? ' sidebar-collapsed' : ''}`}>
        <Sidebar collapsed={collapsed} onToggle={() => setCollapsed((c) => !c)} />
        <button className="sidebar-handle" onClick={() => setCollapsed(false)} title="展开侧栏" />
        <div className="page-main">
          <header className="toolbar">
            <TopNav domain={domain} />
            <div className="toolbar-right">
              <ThemeSwitcher theme={theme} onChange={setTheme} />
            </div>
          </header>
          <main className="content">
            <Outlet />
          </main>
        </div>
      </div>
    </ThemeContext.Provider>
  );
}
```

注意：
- 旧 `ThemeToggle`/`Navbar`/`Brand` 与底部 export 一并删除（ThemeToggle 被 ThemeSwitcher 取代；无外部引用已确认）
- `.page` 上不再需要 `data-theme`（改为 `<html>` 上的 `data-theme`/`data-mode`，由 `applyRootTheme` 管理）

**Step 2: 类型检查**

Run: `cd /Users/aj/workspace/stok-mapping/web/ui && npx tsc --noEmit`
Expected: 通过。

**Step 3: Commit**

```bash
git add web/ui/src/components/Layout.tsx
git commit -m "feat(web/ui): Layout 双层导航重构（侧栏收起 + 顶栏 tab + ThemeSwitcher）"
```

---

## Task 7: 追加双层导航样式（语义 token）

**Objective:** 侧栏/顶栏/占位页/主题菜单样式 + `.page` 改 flex-row；全部用 `--ui-*`。

**Files:**
- Modify: `web/ui/src/index.css`

**Step 1: 改 `.page` 为横向布局**

将:
```css
.page { display: flex; flex-direction: column; height: 100vh; max-width: 1560px; margin: 0 auto; }
```
替换为:
```css
.page { display: flex; flex-direction: row; height: 100vh; max-width: 1560px; margin: 0 auto; }
```

**Step 2: 文件末尾追加**

```css
/* ===== 双层导航：侧栏（第一层）+ 顶栏（第二层）===== */
.sidebar {
  width: 220px;
  flex-shrink: 0;
  display: flex;
  flex-direction: column;
  background: var(--ui-bg-app);
  border-right: 1px solid var(--ui-stroke-tertiary);
  overflow: hidden;
  transition: width 0.25s ease;
}
.page.sidebar-collapsed .sidebar { width: 0; border-right: none; }
.sidebar-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 12px 10px; }
.sidebar-brand { font-weight: 700; font-size: 13px; color: var(--ui-text-primary); white-space: nowrap; text-decoration: none; }
.sidebar-nav { display: flex; flex-direction: column; gap: 2px; padding: 4px 8px; }
.sidebar-item { display: flex; align-items: center; gap: 10px; padding: 8px 10px; border-radius: 6px; color: var(--ui-text-secondary); white-space: nowrap; text-decoration: none; }
.sidebar-item:hover { background: var(--ui-bg-subtle); color: var(--ui-text-primary); }
.sidebar-item.active { background: var(--ui-accent); color: var(--ui-accent-fg); }
.sidebar-icon { font-size: 15px; flex-shrink: 0; }
.sidebar-handle { display: none; width: 8px; flex-shrink: 0; cursor: pointer; padding: 0; border: none; background: var(--ui-bg-app); }
.sidebar-handle:hover { background: var(--ui-accent); opacity: 0.6; }
.page.sidebar-collapsed .sidebar-handle { display: block; }
.page-main { display: flex; flex-direction: column; flex: 1 1 auto; min-width: 0; }
.topnav { display: flex; align-items: center; gap: 2px; flex-wrap: wrap; }
.topnav-link { padding: 6px 12px; border-radius: 6px; font-size: 13.5px; color: var(--ui-text-secondary); text-decoration: none; }
.topnav-link:hover { color: var(--ui-text-primary); background: var(--ui-bg-subtle); }
.topnav-link.active { color: var(--ui-text-primary); font-weight: 600; }
.topnav.loading { animation: navbarPulse 1s ease infinite; }
.placeholder-note { color: var(--ui-text-secondary); font-size: 13.5px; line-height: 1.7; }

/* ===== 主题选择器（浮动层规范）===== */
.theme-switcher { position: relative; }
.theme-menu {
  position: absolute;
  right: 0;
  top: calc(100% + 6px);
  z-index: 50;
  min-width: 160px;
  padding: 4px;
  border-radius: calc(var(--ui-radius-scalar) * 0.5rem);
  background: var(--ui-bg-panel);
  border: 1px solid var(--ui-stroke-nous);
  box-shadow: var(--ui-shadow-nous);
  display: flex;
  flex-direction: column;
  gap: 2px;
}
.theme-menu-item {
  padding: 7px 10px;
  border-radius: calc(var(--ui-radius-scalar) * 0.375rem);
  background: none;
  border: none;
  color: var(--ui-text-secondary);
  font-size: 13px;
  text-align: left;
  cursor: pointer;
}
.theme-menu-item:hover { background: var(--ui-bg-subtle); color: var(--ui-text-primary); }
.theme-menu-item.active { color: var(--ui-text-primary); font-weight: 600; }
```

注意（Flat not boxed）：菜单用 `--ui-stroke-nous` hairline + `--ui-shadow-nous`，无重边框；侧栏无投影。

**Step 3: Commit**

```bash
git add web/ui/src/index.css
git commit -m "style(web/ui): 双层导航 + 主题菜单样式（语义 token，浮动层规范）"
```

---

## Task 8: 路由改造 `App.tsx`

**Objective:** `/` 重定向 `/market/cn`；6 域路由全部就位，未实施域用 PlaceholderPage。

**Files:**
- Modify: `web/ui/src/App.tsx`（整体替换）

**Step 1: 整体重写**

```tsx
import { createBrowserRouter, Navigate, RouterProvider } from 'react-router-dom';
import { Layout } from './components/Layout';
import { PlaceholderPage } from './components/PlaceholderPage';
import { CnSinglePage } from './pages/CnSinglePage';
import { CnDashPage } from './pages/CnDashPage';
import { UsSinglePage } from './pages/UsSinglePage';

/** 路由：Layout 承载侧栏(首层)+顶栏(第二层)，各域页作为 Outlet。
 * 设计依据：docs/WEBSITE_NAVIGATION_STRUCTURE.md §4/§8。 */
const router = createBrowserRouter([
  {
    path: '/',
    element: <Layout />,
    children: [
      // 首页去除：进站直接落到行情默认页
      { index: true, element: <Navigate to="/market/cn" replace /> },

      // 行情（已实现）
      { path: 'market/cn', element: <CnSinglePage /> },
      { path: 'market/dash', element: <CnDashPage /> },
      { path: 'market/us', element: <UsSinglePage /> },

      // 研究（占位）
      { path: 'research/strategies', element: <PlaceholderPage title="策略列表" desc="已注册策略卡片（32 个）——M2 实施" /> },
      { path: 'research/runs/new', element: <PlaceholderPage title="回测运行器" desc="preset / strategy-set / run 配置选择——M2 实施" /> },
      { path: 'research/admission', element: <PlaceholderPage title="准入" desc="admission 窗口矩阵 / 约束审查——M2 实施" /> },
      { path: 'research/runs', element: <PlaceholderPage title="运行对比" desc="run 索引横向对比——M2 实施" /> },

      // 情报（占位；/intel/signals 为信号构建预留）
      { path: 'intel/candidates', element: <PlaceholderPage title="情报候选" desc="采集 / 审查 / 入库——M1 只读实施" /> },
      { path: 'intel/corpus', element: <PlaceholderPage title="语料检索" desc="ai_corpus 新闻 / 公告查询——M1 只读实施" /> },
      { path: 'intel/signals', element: <PlaceholderPage title="信号构建" desc="情报 → 策略信号（预留设计）——M2 实施" /> },

      // 账户（占位；动态刷新 P2）
      { path: 'accounts', element: <PlaceholderPage title="账户总览" desc="模拟账户列表 + 总资产 + 自动刷新开关——P2 实施" /> },
      { path: 'accounts/premarket', element: <PlaceholderPage title="盘前观察池" desc="复用 quant.reporting watchlist——P2 实施" /> },

      // 配置（占位；策略参数编辑器 M1.5 只读版先行）
      { path: 'config/strategies', element: <PlaceholderPage title="策略参数" desc="run 配置编辑器——M1.5 只读版先行" /> },
      { path: 'config/accounts', element: <PlaceholderPage title="账户设置" desc="config.yaml accounts 子集——M2 实施" /> },
      { path: 'config/markets', element: <PlaceholderPage title="行情池" desc="dashboard_indices 标的组——M1.5 实施" /> },
      { path: 'config/system', element: <PlaceholderPage title="系统" desc="数据源 / 环境 / 路径——M2 实施" /> },

      // 观测（占位）
      { path: 'ops/jobs', element: <PlaceholderPage title="任务队列" desc="web_jobs 列表 / 进度——M2 实施" /> },
      { path: 'ops/scheduler', element: <PlaceholderPage title="调度状态" desc="maintain status——M2 实施" /> },
      { path: 'ops/data-health', element: <PlaceholderPage title="数据健康" desc="db-health / 回填审计——M2 实施" /> },

      // 未知 URL 兜底：回行情默认页
      { path: '*', element: <Navigate to="/market/cn" replace /> },
    ],
  },
]);

export function App() {
  return <RouterProvider router={router} />;
}
```

**Step 2: 类型检查**

Run: `cd /Users/aj/workspace/stok-mapping/web/ui && npx tsc --noEmit`
Expected: 通过。

**Step 3: Commit**

```bash
git add web/ui/src/App.tsx
git commit -m "feat(web/ui): 路由改造为六域结构，/ 重定向 /market/cn，未实施域占位"
```

---

## Task 9: 移除 HomePage

**Objective:** 删除首页入口页（决策 1：进站即行情）。

**Files:**
- Delete: `web/ui/src/pages/HomePage.tsx`

**Step 1: 删除文件并确认无引用**

Run: `cd /Users/aj/workspace/stok-mapping && rm web/ui/src/pages/HomePage.tsx && grep -rn "HomePage" web/ui/src --include="*.tsx"`
Expected: grep 无输出。

**Step 2: 类型检查**

Run: `cd /Users/aj/workspace/stok-mapping/web/ui && npx tsc --noEmit`
Expected: 通过。

**Step 3: Commit**

```bash
git add -u web/ui/src/pages/HomePage.tsx
git commit -m "refactor(web/ui): 移除 HomePage（进站直接落到 /market/cn）"
```

---

## Task 10: 构建 + 全量验证

**Objective:** 生产构建通过；后端 8010 托管验证；导航/深链/主题切换/配色目检。

**Step 1: 生产构建**

Run: `cd /Users/aj/workspace/stok-mapping/web/ui && npm run build`
Expected: `tsc -b` 无类型错误，`vite build` 产出 `dist/`，退出码 0。

**Step 2: 启动/重启后端（8010）**

Run: `cd /Users/aj/workspace/stok-mapping && ./restart_web.sh`
（或 `uvicorn web.app.main:app --port 8010`，后台运行）
然后 Run: `curl -s http://127.0.0.1:8010/health`
Expected: `{"ok":true,"db":".../data/a_share_history.sqlite"}`

**Step 3: 深链验证（SPA 回退）**

Run: `curl -s http://127.0.0.1:8010/config/strategies | head -c 200`
Expected: 返回 `dist/index.html` 内容（`<!DOCTYPE html>`），非 404。

**Step 4: 浏览器手动验证**（打开 http://127.0.0.1:8010/）

导航：
- [ ] 访问 `/` 自动跳转 `/market/cn`，A股单标的页正常渲染
- [ ] 侧栏显示 6 域，行情域高亮；顶栏显示行情域 3 个 tab
- [ ] 点击侧栏"配置" → 顶栏切 4 tab，内容区显示"策略参数"占位页
- [ ] 侧栏 ◀ 收起为 0 宽度 + 8px 把手；刷新保持；点击把手展开
- [ ] 深链 `/research/strategies` 刷新不 404，侧栏"研究"高亮；`/nope` 重定向 `/market/cn`

主题切换：
- [ ] 顶栏 🎨 按钮显示当前主题（默认 Nous Dark）；点击弹出 4 项菜单
- [ ] 切 Nous Light → 冷白底 + Nous 蓝 accent；切 Belafonte Night → 深酒红 + 金色；切 Belafonte Day → 暖羊皮纸
- [ ] 每主题下 K线/对照图正常变色（涨红跌绿口径不变）；tooltip/轴/网格线随主题
- [ ] 刷新页面主题保持（`website-theme` key）；旧 `index-chart-theme` key 用户迁移正确（首次加载读旧值 → nous + 对应明暗）

配色目检（Flat not boxed）：
- [ ] 侧栏与内容区同底色 + 单条 hairline，无投影无浮起
- [ ] nous dark 下 MA-30 青 `#6ee7b7` 与跌绿 `#14b143` 可区分（不清晰则调 MA-30 为 `#34d399` 并在 chart/theme.ts 同步）
- [ ] 主题菜单为浮动层（hairline + shadow），无重边框
- [ ] 占位页无卡片嵌套

**Step 5: 收尾 commit（如有构建期修正）**

```bash
git add web/ui/src web/ui/package-lock.json web/ui/tsconfig.tsbuildinfo
git commit -m "chore(web/ui): 导航重构 + 多主题迁移构建验证"
```

---

## 验证清单（最终验收）

- `npm run build` 退出码 0
- `/` → `/market/cn` 重定向生效
- 6 域侧栏 + 域内顶栏 tab 全部工作；收起动画 + localStorage 持久化
- 深链刷新不 404；未知 URL 兜底
- 现有行情三页功能无回归（K线/对照/搜索）
- **4 套主题**（nous/belafonte × dark/light）切换正常，图表随主题变色，涨红跌绿口径未变
- 旧 `index-chart-theme` key 兼容迁移
- git log 包含 10 个独立 commit，无 `git add -A`

## 风险与注意

1. **提交纪律**：每次只 `git add` 本次文件；Task 9 用 `git add -u`；工作区有无关改动
2. **任务 3 中间态 tsc 报错属预期**：theme.ts → ThemeContext → 图表 prop 类型在同任务内完成，最后 tsc 必须通过
3. **CSS 变量惰性解析**：别名只定义在 `:root`，引用 `--ui-*` 随主题块生效——不要在每个主题块重复写别名
4. **`.page` 去掉 `data-theme`**：主题属性移到 `<html>`（applyRootTheme 管理），页面级不再设置
5. **NavLink `end` 语义**：域默认页 tab 加 `end`（防御）
6. **收起把手 8px 偏窄**：目检 hover 高亮，必要时加宽到 10px
7. **nous dark 底偏亮**：MA-30 与跌绿区分度目检，必要时调 `#34d399`（chart/theme.ts 同步）
8. **belafonte night `--ui-stroke-primary #4a3a44`** 为派生值，目检微调
