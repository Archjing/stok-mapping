# Stok Quant Research Scripts

This plugin intentionally starts with skills only.

Use the repository CLI as the deterministic execution layer:

```bash
./.venv/bin/python -m phase0.cli --help
```

Add scripts here only when a workflow is repeated often enough to justify a stable wrapper.
